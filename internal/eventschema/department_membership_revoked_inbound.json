{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DepartmentMembershipRevokedInbound",
  "type": "object",
  "required": [
    "user_id",
    "department_id",
    "role"
  ],
  "additionalProperties": true,
  "properties": {
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "department_id": {
      "type": "string",
      "description": "Format owned by the Org & Membership Service. HandleMembershipRevoked parses it as a UUID today, but that shape is not formally confirmed on Definition's side — see definition_service.md §7.4.2."
    },
    "role": {
      "type": "string",
      "description": "The membership role being revoked."
    }
  }
}
